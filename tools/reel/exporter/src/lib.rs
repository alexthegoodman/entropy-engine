//! The engine's physical models, compiled on their own (no GPU, V8 or window dependencies), plus
//! the brass view's tubing layout.
#![allow(dead_code, unused_imports, clippy::all)]

pub mod audio;
pub mod tubing;
